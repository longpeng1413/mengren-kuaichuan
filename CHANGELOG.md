# 猛人快传版本更新记录

本项目采用“功能完成并经过构建验证后提升版本”的方式记录。早期原型没有逐次保留完整版本说明，因此不虚构缺失的具体小版本号。

## v1.7.0+13 — 2026-08-23

### 修复

- 正式版去除测试版标题，内部构建号提升为 13；超时提示改为要求使用同一正式版本。

- 实机日志确认 VPS 令牌、设备发现和公网转发正常，魅族20对两条 Windows 消息均返回 `decrypt_failed`；明确区分“VPS 访问令牌”和“家庭加密口令”的用途。
- 公网设置中的访问令牌和家庭加密口令都新增显示/隐藏按钮；家庭口令新增仅在本机计算的短校验码，三台设备可直接比较实际内容是否一致。
- 保存和读取访问令牌、家庭口令时统一去除首尾空格，避免复制时混入不可见字符造成解密失败。

- 修复公网发送端只收到 VPS“已转发”就显示发送成功、无法判断接收端是否真正收到的问题；现在必须等到接收端成功解密并处理后才显示成功。
- 公网协议提升为 2，新增接收成功、解密失败、处理失败、接收端断开和送达超时回执；错误会在发送端明确显示，不再静默丢失。
- 公网接收改为严格串行处理，避免图片开始、分块和结束事件并发导致接收顺序错乱。
- 接收端新增不含正文和密钥的元数据诊断日志；无法解密或保存时会显示可见提示。
- 中转服务新增不含明文内容的转发、送达、拒绝和超时日志，便于实机定位。

### 新增

- 公网中转从“仅图片最大 20 MiB”扩展为任意单文件最大 200 MiB，支持视频、APK、压缩包、文档及未知二进制类型。
- 公网文件继续使用 AES-GCM 端到端加密和 256 KiB 分块；保留发送进度、路线标识、接收端自动保存、发送端停止及未完成 `.part` 文件清理。
- 公网协议提升为 3，新增任意文件完整生命周期测试；旧测试版不会与新中转服务混用。

- 新增用户自有 VPS 的在线 WebSocket 密文中转服务和 Android/Windows 共用远程协议包。
- 支持在线远程文字、HTTP/HTTPS 链接和不超过 200 MiB 的任意单文件；文件按最大 256 KiB 分块发送。
- 使用 PBKDF2-HMAC-SHA256 和 AES-GCM-256 端到端加密；家庭口令不发送到 VPS，访问令牌和口令保存到系统安全存储。
- 设置页新增公网远程配置、网络路线说明和公网传输限制；远程设备、设备列表和聊天页明确显示“公网 VPS 中转”。
- 同一设备同时在线时优先使用二维码本地连接或局域网直连，避免绕行公网。
- 公网文件发送沿用停止按钮；取消消息经加密发送，接收端删除未完成 `.part` 文件。
- 新增 Docker 部署文件、TLS 反向代理说明和 `docs/公网远程功能设计-v1.7.0.md`。
- 新增面向用户的 `docs/猛人快传-v1.7.0-安装与使用说明.md`，覆盖 Android/Windows 安装、公网配置、路线识别、5 GHz 热点和常见问题。

### 当前验证

- 远程协议包 analyze 通过，3 项测试通过。
- 在线中转服务 analyze 通过，6 项测试覆盖鉴权、接收端送达确认、解密拒绝、任意文件生命周期、客户端发现和离线拒绝。
- Flutter analyze 通过，24 项应用测试通过；Android/Windows Release 构建成功。
- APK 为 `1.7.0+13`、签名方案 v2，升级证书 SHA-256 仍为 `FE1C0C4965D1C29D66B6EFDE11F403525261152AB53E18E96AB36FEBFE495464`。
- Windows 新增安全存储插件需要 Visual Studio ATL 组件；本机已补装并构建通过。
- 真实 Debian 12 VPS 已部署 `mengren-relay:1.7.0-protocol3`；容器仅绑定 `127.0.0.1:18080`，健康检查、自动重启和不落盘在线模式验证通过。
- 现有个人网站的 Caddy/Ghost 配置未改动，网站复查仍返回 HTTP 200。
- `zlp1314.top` 不在当前 Cloudflare 登录账号的域名列表中，因此按用户授权改用第一个免费托管域名；`relay.meng1314.de5.net` 的仅 DNS A 记录、Caddy TLS、公开健康接口和带令牌 WSS 握手均已验证。
- 未携带访问令牌的 WSS 请求返回 HTTP 401；VPS 环境文件权限为 `root:root 600`。
- VPS 已部署 `mengren-relay:1.7.0-protocol3`；真实 WSS 双客户端完成发现、文字及 APK 文件生命周期的加密、转发、解密和接收端送达确认。
- Windows Release 已验证单实例和 TCP 53318 监听；测试 ZIP 可正常读取 27 个条目。
- 用户确认两台手机升级到公网协议 3 后已恢复 VPS 连接；不同网络下的长期稳定性和接近 200 MiB 文件仍需继续观察。
- 测试版 11 曾出现偶发界面无响应，正式构建尚未复现；若再次出现应优先导出诊断日志继续定位。

## v1.6.1+8 — 2026-08-23

### 新增

- 支持两台 Android 手机通过便携热点组成的本地网络进行自动发现和双向传输。
- Android 原生枚举活动 IPv4 网卡、真实网络前缀和广播地址，覆盖 `ap0`、`wlan0`、`swlan` 等热点/Wi-Fi 接口。
- UDP 发现除 `255.255.255.255` 外，同时向每个可用本地网段发送定向广播。
- 二维码页面显示热点/Wi-Fi 地址、支持刷新网络，并明确支持手机扫描另一台手机。
- 诊断日志记录网卡数量、发现目标变化和单个广播目标失败。
- 设置页新增“连接与高速传输”指南，说明热点连接、二维码兜底、同版本要求、Windows 端口和诊断日志。
- 明确提示大文件使用 5 GHz 热点；用户实测从 2.4 GHz 的约 8 MiB/s 提升到 60–70 MB/s，实际速度取决于设备。

### 兼容性与验证

- 排除蜂窝网络和常见 VPN 网卡，避免把运营商或隧道地址误当作局域网地址。
- 自动发现被手机系统限制时，保留二维码和手动 IP 双向连接兜底。
- 配对协议保持为 2；没有混入计划中的 VPS 公网传输功能。
- Flutter analyze 无问题，20 项测试通过。
- Android/Windows Release 构建成功；APK v2 更新签名、版本号和 Manifest 验证通过。
- 热点双手机实机互传已由用户完成；文字、文件均可发送，5 GHz 热点的大文件速度明显高于 2.4 GHz。

## v1.6.0+7 — 2026-08-22

### 修复

- 修复手机和电脑同时恢复扫码连接时，两条交叉 WebSocket 可能互相关闭的问题。
- 配对 hello 增加双方随机 nonce，并生成一致的 sessionKey；重复连接时两端稳定保留同一条通道。
- 修复聊天页长期保存旧连接类型、配对断开后仍强行使用旧通道的问题。
- 发送前实时检查配对会话；文件尚未开始时可回退到同设备的局域网直连。

### 新增

- 等待接收确认时可由发送端停止。
- 文件传输过程中可由发送端停止。
- 直连取消请求、配对取消、接收确认框关闭及 `.part` 临时文件清理。
- 诊断日志记录局域网设备和配对设备数量变化。

### 协议与验证

- 配对协议升级为 2；v1.5.0 与 v1.6.0 不应混合测试文件传输。
- 新增同时双向重连、直连等待确认取消、配对等待确认取消测试。
- Flutter analyze 无问题，15 项测试通过。
- Android/Windows Release 构建、APK v2 签名和 Windows 单实例验证通过。

## v1.5.0+6 — 2026-08-22

### 新增

- Android 系统分享支持单文件和多文件。
- 支持微信“其他应用打开”（ACTION_VIEW）选择猛人快传。
- Android/Dart 双层诊断日志，可导出、清除、轮换和自动过期。
- 真正的浅色、深色、跟随系统主题，并保留五种强调色。

### 修复与优化

- Windows 单实例：再次点击快捷方式恢复原窗口，不再重复占用 53318。
- Android 分享 Intent 单次消费、单线程后台导入、临时文件原子保存和缓存恢复。
- 发送进度限制到约 10 Hz，避免高速传输时频繁重绘。
- 文件网络块提高到 1 MB，磁盘回压窗口提高到 16 MB。
- 配对 WebSocket 关闭视频/压缩包无效的二次压缩。
- 图片缩略图限制解码尺寸，Flutter 图片缓存限制为 32 MB。
- 本机 Windows 空闲工作集实测约 104.2 MB；不同设备会波动。

### 验证

- Flutter analyze 无问题，12 项测试通过。
- Android/Windows Release 构建成功。
- APK Manifest 确认包含 SEND、SEND_MULTIPLE、VIEW、BROWSABLE。

## v1.4.0+5 — 2026-08-22

### 新增

- 标题显示版本号。
- 五种强调色、Windows 开机自启动、双端默认保存位置。
- 会话清空消息与缓存、移除设备。
- Android 接收文件导出到系统下载或 SAF 自定义目录。
- Android 系统分享入口。
- Windows 关闭到托盘、单击托盘恢复、右键彻底退出。

### 修复与优化

- Android 大文件接收分段写盘和后台系统目录导出。
- 文件气泡保存 content URI，可调用系统“选择打开方式”。
- 写入系统目录成功后删除应用临时副本。

### 保留功能

- Enter 发送、Shift+Enter 换行。
- 双端进度、百分比和速度。
- Windows 拖放文件。
- 文字、链接、文件聊天与本地历史记录。
- 二级路由优先使用双向扫码配对通道。

## 早期原型阶段（v1.4.0 之前）

- 建立 Flutter Android/Windows 双平台工程。
- 实现永久设备 ID 和自定义名称。
- 实现 UDP 53317 自动发现与 TCP 53318 HTTP 文件流。
- 增加二维码及手动 IP/配对码连接，用于二级路由环境。
- 增加聊天窗口、文字、链接、任意文件和接收确认。
- 修复手机可向电脑发送但电脑无法向手机发送的问题，引入双向配对 WebSocket。
- 增加文件发送/接收进度、速度、文件打开和 Windows 拖放。

## 当前未完成事项

- v1.6.0 需要在家庭网络完成手机、四屏电脑、笔记本三设备实机回归。
- v1.7.0 的在线公网密文中转、DNS、TLS 和接收回执已经实现；离线消息、可靠后台通知、超过 200 MiB 的远程文件和点对点加速留待后续版本。
- 暂未实现 S3、账号系统、分块断点续传、正式设备公钥认证和正式安装向导。
- 暂不支持 iOS、macOS、HarmonyOS 独立客户端和 Web 客户端。
